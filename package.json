{
  "name": "barbu",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspace=@barbu/engine && npm run test --workspace=@barbu/party",
    "build": "npm run build --workspaces --if-present"
  }
}
